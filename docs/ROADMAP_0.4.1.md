# CATE 0.4.1 Scope (demo-ready)

Ship ONLY:
1) http-fuzz: --demo preset (deterministic settings + default output)
2) New artifact: <output>.run.md (narrative summary)
3) http-fuzz: --mode presets (recon, waf-behavior)
4) One-command demo runner (make demo or script)

Out of scope:
- UI/dashboard
- major refactors
- new attack surfaces
